export enum ImageType {
    FULL = 'full',
    THUMBNAIL = 'thumbnail',
    GALLERY = 'gallery',
}

export enum ImageStatus {
    PENDING = 'pending',
    READY = 'ready',
    DISABLED = 'disabled'
}
